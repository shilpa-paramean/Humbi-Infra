# ------
# LOCALS
# ------

locals {
  s3_bucket_name          = var.s3_bucket_name
  sftp_server_id          = var.sftp_server_id
  sftp_user_name          = var.sftp_user_name
  ssh_key                 = var.ssh_key
}
